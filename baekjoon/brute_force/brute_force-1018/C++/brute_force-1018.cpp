// #include <bits/stdc++.h>
#include <iostream>
#include <vector>

using namespace std;

// 입력변수생성
int n, m;
int arr[50][50];
// 입력변수생성
int main()
{
    std::ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    freopen("brute_force-1018_input.txt", "r", stdin);

    return 0;
}
